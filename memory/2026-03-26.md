# 2026-03-26 — Инфраструктурный день

## Утро: Проблема с контекстом топиков

**Проблема:** Агент в GenAI топике при "перечитай контекст" выдавал контекст Pepper Potts вместо своего. Потом пытался ставить telethon и сломал pyrogram сессию.

**Причина:** 
- `memory/full-context.md` содержал детали других топиков
- Агент не знал про готовый скрипт `read-topic.py` и писал свой код
- Telethon и Pyrogram несовместимы (разный формат SQLite сессий)

**Решение:**
1. Создан skill `skills/read-topic/SKILL.md` — read-only, только вызов готового скрипта
2. Обновлён `AGENTS.md` — добавлена секция "Запреты" (pip install, написание кода без аппрува)
3. Почищен `memory/full-context.md` — убраны детали топиков
4. Userbot сессия восстановлена из бэкапа

## Группы в конфиге

Добавлены новые группы в `~/.openclaw/openclaw.json`:
- `-1003833640209` — GenAI testing (supergroup, конвертирована из обычной)
- `-1003888103563` — AI T1 pptx test
- `-1003805082954` — DM_channel_test (канал Datamint)

## FAL Image Generation — нестабильность

**Проблема:** FAL провайдер "теряется" через некоторое время после старта gateway.
- Ошибка: "No image-generation provider registered for fal"
- После рестарта gateway — работает
- Через 10-20 минут — опять отваливается

**Временное решение:** Рестарт gateway когда FAL отваливается.

**TODO:** Зарепортить баг на GitHub OpenClaw.

**Также выяснили:**
- `tools.profile: "coding"` не включал image_generate — поменяли на `"full"`
- FAL edit не поддерживает aspectRatio overrides

## Heartbeat и Guest

- Heartbeat для main изменён с 1h на 30m
- Создан `~/.openclaw/exec-approvals.json` с allowlist для guest (read-topic.py)

## Обновление OpenClaw

- Версия обновлена с 2026.3.23-2 до 2026.3.24
- `openclaw doctor --fix` автоматически поправил некоторые конфиги

## SIPNET (из вчерашнего)

Credentials получены утром:
- Логин: 0042727268
- Пароль: sq7Hqg9v
- Trunk зарегистрирован в Asterisk

## ComfyUI — установка (14:38)

**Цель:** Локальная генерация интерьерных рендеров с точным контролем над референсами.

**Статус:** Установка в процессе
- `~/ComfyUI` — репозиторий склонирован
- PyTorch с ROCm 6.2 качается (~3GB)
- Следующие шаги: зависимости ComfyUI, модели (SDXL, ControlNet, IP-Adapter)

**Почему ComfyUI:**
- FAL нестабилен (отваливается каждые 10-20 минут)
- Нужен точный контроль: скетч → рендер с конкретными предметами из референсов
- Maша присылает артикулы плитки/сантехники, нужно точное попадание

**Железо:** Minisforum UM780 Pro (Ryzen 7 7840HS, Radeon 780M iGPU)
- Ожидаемая скорость: 2-5 минут на картинку (vs 10-30 сек в облаке)
- Но полный контроль над результатом

## ТЗ от Маши (GenAI testing)

**Проект:** Рендеры ванной комнаты

**Материалы:**
- Напольная плитка: Equipe Rivoli Bergen Azul (30725)
- Настенная плитка: Equipe Costa Nova White (28454)
- Тумба: IDDIS Edifice (EDI10D0i95K)
- Смеситель: IDDIS AIGSB00i01
- Душевая: IDDIS Aiger AIGSB3Fi06
- Зеркало: IDDIS Uniq UNI1080i98
- Ванна: Jacob Delafon Soissons 160×70
- Полотенцесушитель: Ewrika Penelopa X5

**Требования:**
- Фотореалистично, "как в интерьерном журнале"
- Сочные краски, тёплый свет
- 3 ракурса: фронтальный, план, 3D-перспектива
- НЕ менять расположение предметов

## ComfyUI — завершение установки (15:00-17:00)

**Установлено:**
- PyTorch 2.5.1+ROCm6.2
- ComfyUI v0.18.1
- Модели (~28GB total):
  - RealVisXL V4.0 (6.5GB) — основная для фотореализма
  - SDXL Base 1.0 (6.5GB)
  - SDXL Refiner 1.0 (5.7GB)
  - ControlNet: Canny (2.4GB), Depth (2.4GB), Sketch (739MB)
  - IP-Adapter SDXL (671MB)
  - CLIP Vision (2.4GB)
  - VAE SDXL (320MB)

**Пути:**
- ComfyUI: `~/ComfyUI`
- Venv: `~/ComfyUI/venv`
- API: `http://127.0.0.1:8188`
- Модели: `~/ComfyUI/models/`

**Тестовый рендер на CPU:**
- Время: ~6 минут (320 секунд)
- Качество: фотореалистичное
- Результат: `~/ComfyUI/output/test_bathroom_00001_.png`

**Созданные файлы:**
- Skill: `~/.openclaw/workspace/skills/comfyui-render/SKILL.md`
- Render script: `~/.openclaw/workspace/ops/comfyui-render.py`
- Test script: `~/.openclaw/workspace/ops/comfyui-test.py`
- ТЗ PDF: `/home/dima/.openclaw/media/outbound/genai-tz/TZ-vannaya-FULL.pdf`
- ТЗ DOCX: `/home/dima/.openclaw/media/outbound/genai-tz/TZ-vannaya-v2.docx`

## ROCm — проблемы с GPU (16:40-17:00)

**Установлено:** ROCm 6.2 из repo.radeon.com

**Результат `rocminfo`:**
- CPU: AMD Ryzen 9 7940HS
- GPU: gfx1103 (AMD Radeon 780M)

**Проблема:** GPU Hang при генерации
- ComfyUI запускается, видит GPU (14.9GB VRAM)
- При старте генерации — "HW Exception by GPU node-1, reason: GPU Hang"
- Fatal Python error: Aborted

**Причина:** Известная проблема gfx1103 (Phoenix APU) с PyTorch ROCm 6.2

**Решение:** Пока работаем на CPU (~6 мин/рендер). GPU требует либо патченый ROCm, либо другую версию PyTorch.

## VPN после reboot (16:40)

**Проблема 1:** VPN-Komarovo не поднялся автоматически
- Watchdog в cron есть (`*/15 * * * *`)
- Но nmcli требует sudo → watchdog не может поднять

**Проблема 2:** "Loading gateway certificate failed"
- Сертификат был в `/tmp/isrg-root-x1.pem`
- `/tmp` очистился при reboot!

**Исправление:** Переместить сертификат в `/etc/ssl/certs/isrg-root-x1.pem`

**TODO:** Настроить polkit чтобы dima мог управлять VPN без sudo

## ROCm SIGABRT и CPU-only PyTorch (18:00-18:40)

**Проблема:** ComfyUI падал на 0% генерации без ошибки в логах

**Диагностика:**
- Crash файл в `/var/crash/_usr_bin_python3.12.1000.crash` (6.4GB!)
- Signal 6 = SIGABRT — процесс сам вызвал abort()
- Даже с `--cpu` флагом ROCm PyTorch пытался использовать GPU и падал

**Решение:** Установить чистый CPU-only PyTorch без ROCm:
```bash
cd ~/ComfyUI && source venv/bin/activate
pip uninstall torch torchvision -y
pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
```

**Результат:**
- PyTorch: `2.11.0+cpu` (был `2.5.1+rocm6.2`)
- CUDA available: False
- ROCm: False
- Генерация работает стабильно, ~40 сек/шаг (33 мин на 50 steps)

## Зависшие subagent сессии (18:08-18:23)

**Проблема:** Агент GenAI не отвечал на сообщения

**Причина:** Зависший poll на мёртвых exec сессиях:
- `ember-crest` (pid 22551) — процесс умер, poll в gateway ждал timeout 2400000ms
- `b8a970b4-78e6-47aa-9a7b-7b9cf13fd077` — subagent висел 5+ часов

**Решение:** Рестарт gateway сбрасывает зависшие poll сессии

**Урок:** Нужен механизм отмены poll при падении процесса. Текущий workaround — рестарт gateway.

## Custom nodes установлены

- `ComfyUI_IPAdapter_plus` — для референсов материалов
- `comfyui_controlnet_aux` — для Canny/Depth preprocessing

## Генерация рендера 18:35 — УСПЕХ!

- Prompt ID: `19982364-2709-4c5d-aa2e-5250d856200c`
- Результат: `~/ComfyUI/output/bathroom_FINAL_00001_.png`
- Время: 36 минут
- Параметры: RealVisXL(50) + Refiner(12) + ControlNet Canny(0.4)

**Оценка результата (vision анализ):**
- ✅ Синяя плитка пола — правильно
- ✅ Плетёная корзина — правильно  
- ❌ Тумба — БЕЛАЯ вместо тёмно-серой
- ⚠️ Настенная плитка — горизонтальная вместо вертикальной
- ❌ Полотенцесушитель — отсутствует
- ❌ Штора для ванны — отсутствует

**Соответствие ТЗ: ~33%** — Без IP-Adapter модель игнорирует детали.

## Ванная Маши — новый проект (20:00)

**Структура проекта:** `~/ComfyUI/input/bathroom_masha/`
- ТЗ.md — структурированное задание в формате для скрипта
- скетчи/: front.jpg, angle.jpg, top.jpg
- референсы/: 9 файлов (floor_tiles, wall_tiles, vanity, faucet, mirror, shower, bathtub, towel_warmer, basket)

**Критичные элементы (КРИТИЧНО: ДА):**
- floor (пол)
- wall_tiles (стены)
- vanity (тумба)
- faucet (смеситель)
- mirror (зеркало)
- towel_warmer (полотенцесушитель)

## Исправления comfyui-render.py (20:05)

**Баги найденные при запуске:**
1. ControlNet имя файла: `controlnet-canny-sdxl-1.0.safetensors` → `controlnet-canny-sdxl.safetensors`
2. Canny threshold: `100, 200` → `0.1, 0.4` (норма для ComfyUI)
3. CLIP Vision модель: `CLIP-ViT-bigG-14.safetensors` → `clip_vision_g.safetensors`
4. Пути к файлам: абсолютные → относительные (функция `to_comfyui_path()`)
5. Добавлен флаг `--no-ipadapter` для работы без IP-Adapter (несовместимость CLIP Vision)

**IP-Adapter несовместимость:**
- `ip-adapter_sdxl.safetensors` требует CLIP Vision с размером `[8192, 1280]`
- Имеющийся `clip_vision_g.safetensors` имеет `[8192, 1024]`
- Нужна другая версия: CLIP-ViT-H-14 или IP-Adapter Plus

## Генерация без IP-Adapter (20:10)

- Скрипт: `comfyui-render.py --tz ~/ComfyUI/input/bathroom_masha/ТЗ.md --no-ipadapter`
- Статус: в процессе, 6%
- Сессия: `wild-breeze` (pid 38414)

## Исправление IPAdapterAdvanced (20:18-20:28)

**Ошибка:** "Required input is missing: combine_embeds, embeds_scaling"
- IPAdapterAdvanced node требует дополнительные параметры
- Скрипт не включал их в workflow

**Исправление в comfyui-render.py:**
```python
"combine_embeds": "concat",
"embeds_scaling": "V only"
```

**Ошибка CLIP Vision размерности (20:22):**
- Ошибка: `size mismatch for proj.weight: [8192, 1280] vs [8192, 1024]`
- `clip_vision_g.safetensors` (2.5GB) — размерность 1024
- `ip-adapter_sdxl.safetensors` ожидает размерность 1280

**Решение:** Скачать правильную CLIP Vision модель с HuggingFace:
- URL: `https://huggingface.co/h94/IP-Adapter/resolve/main/sdxl_models/image_encoder/model.safetensors`
- Файл: `CLIP-ViT-bigG-14-laion2B-39B-b160k.safetensors`
- Размер: 3.69GB (правильный)

**Обновление скрипта:**
```python
'clip_name': 'CLIP-ViT-bigG-14-laion2B-39B-b160k.safetensors'  # было clip_vision_g.safetensors
```

## Статус на 20:40

- CLIP Vision скачан: 3.69GB ✅
- Gateway перезапущен ✅
- Готово к запуску рендера с IP-Adapter

**Следующий шаг:** Запуск в GenAI:
```
Прочитай скилл comfyui-render. Запусти рендер по ТЗ:
~/ComfyUI/input/bathroom_masha/ТЗ.md
```

## Крэш системы при рендеринге (22:25-22:30)

**Инцидент:** Полный крэш хоста при тестовом рендере (5 steps)

**Хронология:**
- 22:22 — Запущен рендер через GenAI агента
- 22:25:45 — Последние логи в journalctl
- 22:30:42 — Система перезагрузилась (кнопкой reset)
- Между ними — никаких записей в journal

**Причина:** Kernel panic или OOM killer настолько агрессивный, что не успел записать в лог

**RAM профиль:** 29GB total, потребление на пике ~97.5%

**Решение:** Создан системный монитор для диагностики

## Системный монитор (22:33)

**Создан:** `~/.openclaw/workspace/ops/system-monitor.sh`

**Функции:**
- Логирует каждые 5 сек в JSONL
- RAM used/available/percent
- Swap usage
- CPU load (1m, 5m)
- ComfyUI process status и RSS
- Алерт если RAM > 90%

**Запуск:**
```bash
nohup ~/.openclaw/workspace/ops/system-monitor.sh /tmp/sysmon.jsonl 5 > /tmp/sysmon.log 2>&1 &
```

**Лог файл:** `/tmp/sysmon.jsonl`

## Обновление skill для безопасности (22:35-22:36)

**Добавлено в SKILL.md:**
1. Обязательный запуск монитора перед ComfyUI
2. Флаг `--disable-smart-memory` — выгружает модели после использования
3. Инструкции по проверке лога монитора

**Команда запуска:**
```bash
cd ~/ComfyUI && source venv/bin/activate
nohup python main.py --listen --port 8188 --cpu --disable-smart-memory > /tmp/comfyui.log 2>&1 &
```

## Тестовый рендер с мониторингом (22:38-22:45)

**Профиль памяти:**
- 22:38 — Старт: RAM 2.7GB (9%)
- 22:39:13 — CLIP загружен: RAM 19GB (65%)
- 22:39:18 — CLIP Vision: RAM 22.8GB (78%), Swap началось
- 22:39:43 — Пик при генерации: RAM 28GB (96%), Swap 8GB (полный)
- 22:41:14 — Step 1/5 завершён за 1:30
- 22:43:46 — Step 5/5 завершён за 3:59 total ✅
- 22:45:26 — Загрузка Refiner: RAM 97.5%, Load 61!
- 22:45:31 — OOM killed: RAM упал до 1.6GB, ComfyUI stopped

**Вывод:** 
- Основная генерация (5 steps) проходит успешно
- **Refiner не помещается** — при загрузке OOM killer убивает процесс
- Нужен флаг `--no-refiner`

**Флаг `--disable-smart-memory` НЕ помог** — модели всё равно держатся в RAM одновременно при загрузке Refiner

## TODO

- [ ] Добавить `--no-refiner` флаг в скрипт
- [ ] Успешный полный рендер без Refiner
- [ ] Если результат приемлемый без Refiner — оставить так
- [ ] Альтернатива: увеличить swap до 16-32GB
